name: Main Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    # defaults:
    #   run:
    #     working-directory: ./app

    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      #- name: install app dependencies
      #  working-directory: ./app
      #  run: flutter pub get
#
      #- name: Run tests
      #  working-directory: ./app
      #  run: flutter test
#
      #- name: Build flutter web
      #  working-directory: ./app
      #  run: flutter build web

      - name: Find Working Directory
        run: pwd
      - name: Build Web
        uses: ./.github/workflows/build-phase.yml
        with:
          app_dir: ./app
      #- name: Deploy
      #  uses: ./.github/workflows/deploy-phase.yml
